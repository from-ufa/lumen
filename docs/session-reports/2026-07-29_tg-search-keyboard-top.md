# TG search keyboard → pin to top

**Commit:** `bf93c73`  

On Telegram focus: scroll + `position:fixed` to `visualViewport` top so search stays upper screen while keyboard is open (not mid-screen). Non-TG unchanged.
