# Remove AUTO · 5s from Oracles header

**Commit:** `340165d` on `main`  
**Change:** deleted badge in `app/oracles/page.tsx` (all clients: web / mobile / TG share same page)  
**Deploy:** build + restart · oracles 200 · badge absent from HTML  
