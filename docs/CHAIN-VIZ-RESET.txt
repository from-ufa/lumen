CHAIN VISUALIZATION — CLEAN SLATE (2026-07-26)
==============================================

STATUS: blank page for graphics + look decisions.

REMOVED (graphics / look only):
- app/components/ChainPulse.tsx
- Dashboard tab CHAIN PULSE
- All prior visual concepts (Block Genesis, Crystallogenesis, soft sprites,
  crystal core, sealed stack dock, assemble pulse, chime, storyboard HUD, …)
- Design prescriptions purged from THIN-EXPLORER-CAPABILITIES.txt

KEPT (data plane — thin explorer):
- lib/chain.ts
- app/api/chain/*  (status, feed, blocks, block, mempool, address, token)

ALSO KEPT (not chain viz):
- Network Orbit (Constellation3D) + World Map
- BlocksTimeline / MempoolFlow sidebar lists

RULE: next chain visual starts from zero. Data tags (mempool/sealed/focus)
are API shape only — not a mandate for how anything looks.

Next: design new chain visualization when product direction is set.
