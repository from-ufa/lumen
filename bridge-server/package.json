{
  "name": "lumen-bridge-server",
  "version": "1.0.0",
  "description": "Lumen Bridge backend — WebSocket hub + token API + node proxy",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test:smoke": "node scripts/smoke-test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "ws": "^8.18.3"
  }
}
